# netsniffer
This tool captures network packets in real-time and analyzes different layers such as IP and TCP. It also extracts raw data payloads, which can help in understanding traffic patterns and detecting suspicious activities.
